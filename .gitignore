_local/
.DS_Store
*.mov
*.mp4
*.zip

# credenziali
.env
.env.*

# stato locale di Obsidian (non è contenuto)
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# skills di terze parti installate con `npx skills add`: le ripristina skills-lock.json
.claude/skills/
.agents/skills/

# ⚠️ Dati finanziari personali: reddito, patrimonio, spese, investimenti.
# Esclusi dal 01/09/2026, PRIMA che l'area esistesse e prima di qualsiasi push.
# Vivono solo su questo disco. Se un giorno servisse versionarli, si fa in un
# repository privato separato — non qui, dove il remote è già in piedi.
areas/finanza/
