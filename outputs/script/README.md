# Script video

I copioni prodotti dalla skill [`crea-contenuto`](../../code/skills/crea-contenuto/SKILL.md).

Un file per copione, `<AAAA-MM-GG>-<slug>.md`: dentro ci sono i tre hook fra cui scegliere, lo
sviluppo con i punti di rottura, la CTA, la lista delle riprese e i materiali da procurarsi.

Il copione è un deliverable, non un appunto: si porta dietro il giorno che si gira.
